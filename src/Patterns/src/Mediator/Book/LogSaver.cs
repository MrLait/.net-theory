namespace Mediator.Book
{
    class LogSaver
    {
        public void SaveLogEntry(string fileName)
        {
            System.Console.WriteLine($"{fileName}: содержимае сохранено");
        }
    }
}
